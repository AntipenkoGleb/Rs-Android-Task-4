<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <data>

        <import type="by.agd.rsandroidtask4.binding.ViewBindingConverter" />

        <variable
            name="viewModel"
            type="by.agd.rsandroidtask4.view.addcar.AddCarViewModel" />

        <variable
            name="selectImageClickListener"
            type="android.view.View.OnClickListener" />

        <variable
            name="addCarClickListener"
            type="by.agd.rsandroidtask4.view.addcar.OnAddCarClickListener" />
    </data>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <include
                android:id="@+id/car_details_layout"
                layout="@layout/car_details"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:selectImageClickListener="@{selectImageClickListener}"
                app:viewModel="@{viewModel}" />

            <com.google.android.material.button.MaterialButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:onClick="@{() -> addCarClickListener.onClick(viewModel.car)}"
                android:text="Add car"
                app:icon="@drawable/ic_add_car"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/car_details_layout" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>
</layout>